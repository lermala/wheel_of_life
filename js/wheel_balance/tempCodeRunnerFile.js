                // // // currentPalette.innerText = text;
                // // currentPalette.querySelector('.palette').remove();
                // // currentPalette.appendChild(clickedPalette);                
                // // this.closest('.select').classList.remove('is-active'); // closing